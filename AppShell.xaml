<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TodoApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:TodoApp"
    xmlns:pages="clr-namespace:TodoApp.Pages"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <Tab Icon="todo.svg">
            <ShellContent ContentTemplate="{DataTemplate local:MainPage}"/>
        </Tab>
        <Tab Icon="calendar.svg">
            <ShellContent ContentTemplate="{DataTemplate pages:HomeWorkPage}"/>
        </Tab>
        <!--<Tab Title="Test">
            <ShellContent ContentTemplate="{DataTemplate pages:LifeManagerPage}"/>
        </Tab>-->
    </TabBar>
    


</Shell>
